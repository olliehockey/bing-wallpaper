# Windows

Windows support is planned for this repository.

When implemented, the Windows version will live in this folder and will be installed explicitly from PowerShell:

    cd windows
    .\install.ps1

The macOS installer does not use this folder.
